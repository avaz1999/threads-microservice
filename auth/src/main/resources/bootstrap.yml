spring:
  application:
    name: auth
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: 123
      username: user
  profiles:
#    active: local
    active: dev
