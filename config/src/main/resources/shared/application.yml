security:
  oauth2:
    client:
      client-id: mf-client
      client-secret: 123
      accessTokenUri: http://auth/oauth/token
    resource:
      user-info-uri: http://auth/user/current
      token-info-uri: http://auth/oauth/check_token
      prefer-token-info: false
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka